import PageMain from "@/components/layout/page/main/PageMain";
import React from "react";

const BiddingLayout = ({ children }: { children: React.ReactNode }) => {
    return <PageMain>{children}</PageMain>;
};

export default BiddingLayout;
